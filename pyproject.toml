[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dspark"
version = "0.1.0"
description = "A dependency-free, memory-mapped, quantized inference runtime for the Ornith model family."
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
dependencies = []

[project.scripts]
dspark = "dspark.cli:main"

[tool.setuptools.packages.find]
include = ["dspark*"]
